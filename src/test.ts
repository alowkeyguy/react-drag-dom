import Dragger, {DragFrame} from './index'


describe('Dragger', () => {
  it('is truthy', () => {
    expect(Dragger).toBeTruthy()
  })
})

describe('DragFrame', () => {
  it('is truthy', () => {
    expect(DragFrame).toBeTruthy()
  })
})
